import Landing from './Landing';

export const metadata = {
  title: 'Portia — Cada edificio merece estar en orden',
  description: 'Conserjería digital para edificios residenciales en Chile. El conserje registra, el administrador observa, y el edificio entero funciona con la misma información en tiempo real.',
};

export default function Home() {
  return <Landing />;
}
