{
  "name": "portia",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "desktop": "npm run dev --workspace=apps/desktop"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "electron-builder": "^25.1.8",
    "app-builder-bin": "*"
  }
}
